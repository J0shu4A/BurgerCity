# Lokal (Windows-Dev): Vite-Dev-Server auf 5173, Express auf 5174.
VITE_API_BASE=http://localhost:5174

# Auf Linux-Server in Production NICHT setzen - dann liefert
# der Express auf demselben Port das Frontend mit aus (same-origin).
# FastAPI default = https://seiz.ing
# VITE_FASTAPI_BASE=https://seiz.ing
